# Password Change Prompt

You must add the `<?php include __DIR__ . '/../../components/password_change_popup.php'; ?>` line in the pages where you want the password change popup to appear.

# Migrations

After adding the main sql ddl (`/database/schema.sql`), you must also run the migration files or just rewrite the ddl file directly.